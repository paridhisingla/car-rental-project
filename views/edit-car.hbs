<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Car</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f4f4;
    }

    .edit-container {
      max-width: 600px;
      margin: 60px auto;
      background: white;
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
    }

    h1 {
      text-align: center;
      color: #5e60ce;
      margin-bottom: 30px;
      font-size: 32px;
    }

    label {
      display: block;
      margin-bottom: 6px;
      font-weight: bold;
      color: #333;
    }

    input, select {
      width: 100%;
      padding: 12px;
      margin-bottom: 20px;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 16px;
    }

    button {
      width: 100%;
      padding: 12px;
      background-color: #5e60ce;
      color: white;
      font-size: 18px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    button:hover {
      background-color: #3f41b5;
    }
  </style>
</head>
<body>

  <div class="edit-container">
    <h1>Edit Car - {{car.name}}</h1>
    <form action="/cars/{{car.id}}?_method=POST" method="POST">
      <label for="name">Name</label>
      <input type="text" id="name" name="name" value="{{car.name}}" required>

      <label for="brand">Brand</label>
      <input type="text" id="brand" name="brand" value="{{car.brand}}" required>

      <label for="pricePerDay">Price Per Day</label>
      <input type="number" id="pricePerDay" name="pricePerDay" value="{{car.pricePerDay}}" required>

      <label for="available">Available</label>
      <select id="available" name="available">
        <option value="true" {{#if car.available}}selected{{/if}}>Yes</option>
        <option value="false" {{#unless car.available}}selected{{/unless}}>No</option>
      </select>

      <button type="submit">Update Car</button>
    </form>
  </div>

</body>
</html>
