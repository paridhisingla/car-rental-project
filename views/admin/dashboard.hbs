<!DOCTYPE html>
<html>
<head>
    <title>Admin Dashboard - Elite Rentals</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background-color: #000;
            font-family: 'Montserrat', sans-serif;
            color: #fff;
        }
        
        .admin-sidebar {
            background-color: #111;
            min-height: 100vh;
            border-right: 1px solid #333;
            position: fixed;
            width: 250px;
            padding-top: 80px;
        }
        
        .admin-logo {
            color: #c9a959;
            font-size: 1.5rem;
            font-weight: 600;
            text-align: center;
            margin-bottom: 30px;
            letter-spacing: 2px;
        }
        
        .admin-menu {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        
        .admin-menu li {
            margin-bottom: 5px;
        }
        
        .admin-menu a {
            display: block;
            padding: 12px 20px;
            color: #fff;
            text-decoration: none;
            transition: all 0.3s ease;
            border-left: 3px solid transparent;
        }
        
        .admin-menu a:hover, .admin-menu a.active {
            background-color: #1a1a1a;
            border-left-color: #c9a959;
            color: #c9a959;
        }
        
        .admin-menu i {
            margin-right: 10px;
            width: 20px;
            text-align: center;
        }
        
        .admin-content {
            margin-left: 250px;
            padding: 30px;
        }
        
        .admin-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 15px;
            border-bottom: 1px solid #333;
        }
        
        .admin-title {
            color: #c9a959;
            font-size: 2rem;
            font-weight: 600;
            margin: 0;
        }
        
        .admin-user {
            display: flex;
            align-items: center;
        }
        
        .admin-user-name {
            margin-right: 15px;
            color: #fff;
        }
        
        .admin-card {
            background-color: #111;
            border: 1px solid #333;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 30px;
        }
        
        .admin-card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 1px solid #333;
        }
        
        .admin-card-title {
            color: #c9a959;
            font-size: 1.5rem;
            margin: 0;
        }
        
        .admin-stats {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .admin-stat-card {
            background-color: #1a1a1a;
            border: 1px solid #333;
            border-radius: 5px;
            padding: 20px;
            display: flex;
            align-items: center;
        }
        
        .admin-stat-icon {
            width: 60px;
            height: 60px;
            background-color: #c9a959;
            color: #000;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            margin-right: 15px;
        }
        
        .admin-stat-info h3 {
            font-size: 1.8rem;
            margin: 0 0 5px 0;
            color: #fff;
        }
        
        .admin-stat-info p {
            margin: 0;
            color: #999;
            font-size: 0.9rem;
        }
        
        .admin-table {
            width: 100%;
            border-collapse: collapse;
        }
        
        .admin-table th {
            background-color: #1a1a1a;
            color: #c9a959;
            text-align: left;
            padding: 12px 15px;
            font-size: 0.9rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .admin-table td {
            padding: 12px 15px;
            border-top: 1px solid #333;
        }
        
        .admin-table tr:hover {
            background-color: #1a1a1a;
        }
        
        .admin-table .car-image {
            width: 60px;
            height: 40px;
            object-fit: cover;
            border-radius: 3px;
        }
        
        .admin-actions {
            display: flex;
            gap: 5px;
        }
        
        .admin-actions a, .admin-actions button {
            width: 32px;
            height: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 3px;
            color: #fff;
            text-decoration: none;
            border: none;
            cursor: pointer;
        }
        
        .admin-actions .view {
            background-color: #0d6efd;
        }
        
        .admin-actions .edit {
            background-color: #ffc107;
        }
        
        .admin-actions .delete {
            background-color: #dc3545;
        }
        
        .admin-badge {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 3px;
            font-size: 0.8rem;
            font-weight: 600;
        }
        
        .admin-badge.available {
            background-color: #198754;
            color: #fff;
        }
        
        .admin-badge.booked {
            background-color: #dc3545;
            color: #fff;
        }
        
        .admin-badge.pending {
            background-color: #ffc107;
            color: #000;
        }
    </style>
</head>
<body>
    <div class="admin-sidebar">
        <div class="admin-logo">ELITE RENTALS</div>
        <ul class="admin-menu">
            <li><a href="/admin/dashboard" class="active"><i class="fas fa-tachometer-alt"></i> Dashboard</a></li>
            <li><a href="/admin/cars"><i class="fas fa-car"></i> Cars</a></li>
            <li><a href="/admin/bookings"><i class="fas fa-calendar-check"></i> Bookings</a></li>
            <li><a href="/admin/users"><i class="fas fa-users"></i> Users</a></li>
            <li><a href="/admin/settings"><i class="fas fa-cog"></i> Settings</a></li>
            <li><a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
        </ul>
    </div>
    
    <div class="admin-content">
        <div class="admin-header">
            <h1 class="admin-title">Dashboard</h1>
            <div class="admin-user">
                <span class="admin-user-name">Welcome, {{user.name}}</span>
                <a href="/profile" class="btn btn-outline-light btn-sm">Profile</a>
            </div>
        </div>
        
        <div class="admin-stats">
            <div class="admin-stat-card">
                <div class="admin-stat-icon">
                    <i class="fas fa-car"></i>
                </div>
                <div class="admin-stat-info">
                    <h3>{{totalCars}}</h3>
                    <p>Total Cars</p>
                </div>
            </div>
            
            <div class="admin-stat-card">
                <div class="admin-stat-icon">
                    <i class="fas fa-users"></i>
                </div>
                <div class="admin-stat-info">
                    <h3>{{totalUsers}}</h3>
                    <p>Registered Users</p>
                </div>
            </div>
            
            <div class="admin-stat-card">
                <div class="admin-stat-icon">
                    <i class="fas fa-calendar-check"></i>
                </div>
                <div class="admin-stat-info">
                    <h3>{{totalBookings}}</h3>
                    <p>Total Bookings</p>
                </div>
            </div>
            
            <div class="admin-stat-card">
                <div class="admin-stat-icon">
                    <i class="fas fa-dollar-sign"></i>
                </div>
                <div class="admin-stat-info">
                    <h3>${{totalRevenue}}</h3>
                    <p>Total Revenue</p>
                </div>
            </div>
        </div>
        
        <div class="admin-card">
            <div class="admin-card-header">
                <h2 class="admin-card-title">Recent Cars</h2>
                <a href="/admin/cars" class="btn btn-outline-primary btn-sm">View All</a>
            </div>
            
            <table class="admin-table">
                <thead>
                    <tr>
                        <th>Image</th>
                        <th>Name</th>
                        <th>Category</th>
                        <th>Price/Day</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each cars}}
                    <tr>
                        <td><img src="{{this.image}}" alt="{{this.name}}" class="car-image"></td>
                        <td>{{this.name}}</td>
                        <td>{{this.category}}</td>
                        <td>${{this.pricePerDay}}</td>
                        <td>
                            {{#if this.available}}
                            <span class="admin-badge available">Available</span>
                            {{else}}
                            <span class="admin-badge booked">Booked</span>
                            {{/if}}
                        </td>
                        <td>
                            <div class="admin-actions">
                                <a href="/cars/{{this._id}}" class="view"><i class="fas fa-eye"></i></a>
                                <a href="/admin/cars/{{this._id}}/edit" class="edit"><i class="fas fa-edit"></i></a>
                                <form action="/admin/cars/{{this._id}}/delete" method="POST" style="display: inline;">
                                    <button type="submit" class="delete" onclick="return confirm('Are you sure you want to delete this car?')"><i class="fas fa-trash"></i></button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        
        <div class="admin-card">
            <div class="admin-card-header">
                <h2 class="admin-card-title">Recent Bookings</h2>
                <a href="/admin/bookings" class="btn btn-outline-primary btn-sm">View All</a>
            </div>
            
            <table class="admin-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>User</th>
                        <th>Car</th>
                        <th>Start Date</th>
                        <th>End Date</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each bookings}}
                    <tr>
                        <td>{{this._id}}</td>
                        <td>{{this.user.name}}</td>
                        <td>{{this.car.name}}</td>
                        <td>{{formatDate this.startDate}}</td>
                        <td>{{formatDate this.endDate}}</td>
                        <td>
                            <span class="admin-badge {{this.status}}">{{this.status}}</span>
                        </td>
                        <td>
                            <div class="admin-actions">
                                <a href="/bookings/{{this._id}}" class="view"><i class="fas fa-eye"></i></a>
                                <a href="/admin/bookings/{{this._id}}/edit" class="edit"><i class="fas fa-edit"></i></a>
                            </div>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>