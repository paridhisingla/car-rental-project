<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{car.name}} - Details</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 40px;
      background-color: #f3f3f3;
    }

    .car-detail {
      display: flex;
      max-width: 900px;
      margin: auto;
      background: #fff;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    .car-image {
      width: 45%;
      object-fit: cover;
    }

    .car-info {
      padding: 30px;
      width: 55%;
    }

    .car-info h2 {
      margin-top: 0;
      color: #333;
      margin-bottom: 15px;
    }

    .car-info p {
      margin: 10px 0;
      font-size: 16px;
      color: #555;
    }

    .car-info .price {
      font-size: 18px;
      font-weight: bold;
      color: #28a745;
      margin-top: 20px;
    }

    .btn-group {
      margin-top: 30px;
    }

    .btn-group a,
    .btn-group form button {
      display: inline-block;
      padding: 10px 16px;
      margin-right: 10px;
      text-decoration: none;
      font-size: 14px;
      border-radius: 4px;
      cursor: pointer;
      border: none;
    }

    .book-btn {
      background-color: #007bff;
      color: white;
    }

    .edit-btn {
      background-color: #ffc107;
      color: black;
    }

    .delete-btn {
      background-color: #dc3545;
      color: white;
    }

    .back-link {
      text-align: center;
      margin-top: 30px;
    }

    .back-link a {
      text-decoration: none;
      color: #007bff;
      font-size: 16px;
    }

    .back-link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="car-detail">
    <img class="car-image" src="{{car.image}}" alt="{{car.name}}" onerror="this.src='https://source.unsplash.com/500x400/?car,{{car.brand}}'" />
    
    <div class="car-info">
      <h2>{{car.name}}</h2>
      <p><strong>Brand:</strong> {{car.brand}}</p>
      <p><strong>Car ID:</strong> {{car.id}}</p>
      <p><strong>Available:</strong> {{#if car.available}}✅ Yes{{else}}❌ No{{/if}}</p>
      <p class="price"><strong>Price per Day:</strong> ${{car.pricePerDay}}</p>

      <div class="btn-group">
        <a class="book-btn" href="/bookings/new?carId={{car.id}}">Book Now</a>
        <a class="edit-btn" href="/cars/{{car.id}}/edit">Edit</a>
        <form action="/cars/{{car.id}}?_method=DELETE" method="POST" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete this car?');">
          <button type="submit" class="delete-btn">Delete</button>
        </form>
      </div>
    </div>
  </div>

  <div class="back-link">
    <a href="/cars">← Back to Car List</a>
  </div>

</body>
</html>
