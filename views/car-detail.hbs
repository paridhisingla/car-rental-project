<!DOCTYPE html>
<html>
<head>
    <title>{{car.name}} - Elite Rentals</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background-color: #000;
            font-family: 'Playfair Display', serif;
            color: #fff;
        }
        .navbar {
            background: rgba(0, 0, 0, 0.8);
            padding: 1.5rem 0;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            backdrop-filter: blur(10px);
        }
        .navbar-brand {
            color: #c9a959 !important;
            font-size: 1.8rem;
            font-weight: 400;
            letter-spacing: 2px;
        }
        .car-detail-section {
            margin-top: 100px;
            padding: 2rem 0;
        }
        .car-image {
            width: 100%;
            height: 400px;
            object-fit: cover;
            margin-bottom: 2rem;
        }
        .car-title {
            color: #c9a959;
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }
        .car-brand {
            color: #fff;
            font-size: 1.5rem;
            margin-bottom: 2rem;
        }
        .car-features {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        .feature-item {
            background: #111;
            padding: 1.5rem;
            border: 1px solid #333;
        }
        .feature-icon {
            color: #c9a959;
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
        .feature-label {
            color: #999;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }
        .feature-value {
            color: #fff;
            font-size: 1.2rem;
        }
        .car-description {
            color: #999;
            font-size: 1.1rem;
            line-height: 1.8;
            margin-bottom: 2rem;
        }
        .booking-section {
            background: #111;
            padding: 2rem;
            border: 1px solid #333;
        }
        .booking-title {
            color: #c9a959;
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
        }
        .form-label {
            color: #c9a959;
            font-size: 1.1rem;
            margin-bottom: 0.5rem;
        }
        .form-control {
            background: #000;
            border: 1px solid #333;
            color: #fff;
            padding: 12px;
            border-radius: 0;
        }
        .form-control:focus {
            background: #000;
            border-color: #c9a959;
            color: #fff;
            box-shadow: none;
        }
        .btn-book {
            background: #c9a959;
            border: none;
            color: #000;
            padding: 15px 30px;
            border-radius: 0;
            font-weight: 500;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            width: 100%;
            margin-top: 1rem;
        }
        .btn-book:hover {
            background: #b38f4a;
            transform: translateY(-2px);
        }
        .price-summary {
            background: #111;
            padding: 1.5rem;
            border: 1px solid #333;
            margin-top: 1rem;
        }
        .price-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1rem;
            color: #999;
        }
        .price-total {
            display: flex;
            justify-content: space-between;
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid #333;
            color: #c9a959;
            font-size: 1.2rem;
        }
        .reviews-section {
            margin-top: 3rem;
        }
        .review-card {
            background: #111;
            padding: 1.5rem;
            border: 1px solid #333;
            margin-bottom: 1rem;
        }
        .review-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1rem;
        }
        .review-author {
            color: #c9a959;
            font-size: 1.1rem;
        }
        .review-rating {
            color: #c9a959;
        }
        .review-content {
            color: #999;
            line-height: 1.6;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">ELITE RENTALS</a>
        </div>
    </nav>

<<<<<<< Updated upstream
    <div class="container car-detail-section">
        <div class="row">
            <div class="col-md-8">
                <img src="{{car.image}}" alt="{{car.name}}" class="car-image">
                <h1 class="car-title">{{car.name}}</h1>
                <h2 class="car-brand">{{car.brand}}</h2>
                
                <div class="car-features">
                    <div class="feature-item">
                        <div class="feature-icon"><i class="fas fa-cog"></i></div>
                        <div class="feature-label">Transmission</div>
                        <div class="feature-value">{{car.transmission}}</div>
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon"><i class="fas fa-gas-pump"></i></div>
                        <div class="feature-label">Fuel Type</div>
                        <div class="feature-value">{{car.fuelType}}</div>
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon"><i class="fas fa-chair"></i></div>
                        <div class="feature-label">Seats</div>
                        <div class="feature-value">{{car.seats}}</div>
                    </div>
                    <div class="feature-item">
                        <div class="feature-icon"><i class="fas fa-tag"></i></div>
                        <div class="feature-label">Category</div>
                        <div class="feature-value">{{car.category}}</div>
                    </div>
                </div>
=======
  <div class="car-detail">
    <img class="car-image" src="{{car.image}}" alt="{{car.name}}" onerror="this.src='https://source.unsplash.com/500x400/?car,{{car.brand}}'" />
    
    <div class="car-info">
      <h2>{{car.name}}</h2>
      <p><strong>Brand:</strong> {{car.brand}}</p>
      <p><strong>Car ID:</strong> {{car.id}}</p>
      <p><strong>Available:</strong> {{#if car.available}}✅ Yes{{else}}❌ No{{/if}}</p>
      <p class="price"><strong>Price per Day:</strong> ${{car.pricePerDay}}</p>
>>>>>>> Stashed changes

                <p class="car-description">
                    Experience luxury and performance with the {{car.name}}. This {{car.brand}} masterpiece combines elegant design with powerful engineering, offering an unforgettable driving experience. Perfect for both city cruising and long-distance journeys.
                </p>

                <div class="reviews-section">
                    <h3 class="booking-title">Customer Reviews</h3>
                    {{#each car.reviews}}
                    <div class="review-card">
                        <div class="review-header">
                            <div class="review-author">{{userName}}</div>
                            <div class="review-rating">
                                {{#times rating}}
                                <i class="fas fa-star"></i>
                                {{/times}}
                            </div>
                        </div>
                        <div class="review-content">{{comment}}</div>
                    </div>
                    {{/each}}
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="booking-section">
                    <h3 class="booking-title">Book This Car</h3>
                    <form action="/bookings" method="POST">
                        <input type="hidden" name="carId" value="{{car.id}}">
                        
                        <div class="mb-3">
                            <label for="userName" class="form-label">Your Name</label>
                            <input type="text" class="form-control" id="userName" name="userName" required>
                        </div>
                        
                        <div class="mb-3">
                            <label for="startDate" class="form-label">Start Date</label>
                            <input type="date" class="form-control" id="startDate" name="startDate" required>
                        </div>
                        
                        <div class="mb-3">
                            <label for="endDate" class="form-label">End Date</label>
                            <input type="date" class="form-control" id="endDate" name="endDate" required>
                        </div>

                        <div class="price-summary">
                            <div class="price-item">
                                <span>Price per day:</span>
                                <span>${{car.pricePerDay}}</span>
                            </div>
                            <div class="price-item">
                                <span>Number of days:</span>
                                <span id="days">0</span>
                            </div>
                            <div class="price-total">
                                <span>Total price:</span>
                                <span id="totalPrice">$0</span>
                            </div>
                        </div>

                        <button type="submit" class="btn btn-book" {{#unless car.available}}disabled{{/unless}}>
                            {{#if car.available}}BOOK NOW{{else}}NOT AVAILABLE{{/if}}
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Calculate total price based on selected dates
        const startDate = document.getElementById('startDate');
        const endDate = document.getElementById('endDate');
        const daysElement = document.getElementById('days');
        const totalPriceElement = document.getElementById('totalPrice');
        const pricePerDay = {{car.pricePerDay}};

        function calculatePrice() {
            if (startDate.value && endDate.value) {
                const start = new Date(startDate.value);
                const end = new Date(endDate.value);
                const diffTime = Math.abs(end - start);
                const days = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
                
                daysElement.textContent = days;
                totalPriceElement.textContent = `$${days * pricePerDay}`;
            }
        }

        startDate.addEventListener('change', calculatePrice);
        endDate.addEventListener('change', calculatePrice);
    </script>
</body>
</html>
