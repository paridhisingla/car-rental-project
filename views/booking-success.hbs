<!DOCTYPE html>
<html>
<head>
    <title>Booking Confirmed - Elite Rentals</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background-color: #000;
            font-family: 'Playfair Display', serif;
            color: #fff;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .navbar {
            background: rgba(0, 0, 0, 0.8);
            padding: 1.5rem 0;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            backdrop-filter: blur(10px);
        }
        .navbar-brand {
            color: #c9a959 !important;
            font-size: 1.8rem;
            font-weight: 400;
            letter-spacing: 2px;
        }
        .success-container {
            background: #111;
            border: 1px solid #333;
            padding: 3rem;
            width: 100%;
            max-width: 600px;
            margin-top: 80px;
            text-align: center;
        }
        .success-icon {
            color: #c9a959;
            font-size: 4rem;
            margin-bottom: 2rem;
        }
        .success-title {
            color: #c9a959;
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            letter-spacing: 2px;
        }
        .success-message {
            color: #fff;
            font-size: 1.2rem;
            margin-bottom: 2rem;
            line-height: 1.6;
        }
        .booking-details {
            background: #000;
            border: 1px solid #333;
            padding: 2rem;
            margin-bottom: 2rem;
        }
        .detail-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1rem;
            color: #999;
        }
        .detail-item:last-child {
            margin-bottom: 0;
            padding-top: 1rem;
            border-top: 1px solid #333;
            color: #c9a959;
            font-size: 1.2rem;
        }
        .btn-primary {
            background: #c9a959;
            border: none;
            color: #000;
            padding: 12px 30px;
            border-radius: 0;
            font-weight: 500;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }
        .btn-primary:hover {
            background: #b38f4a;
            color: #000;
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">ELITE RENTALS</a>
        </div>
    </nav>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="success-container">
                    <div class="success-icon">
                        <i class="fas fa-check-circle"></i>
                    </div>
                    <h1 class="success-title">BOOKING CONFIRMED</h1>
                    <p class="success-message">
                        Thank you for choosing Elite Rentals. Your booking has been successfully confirmed.
                        We look forward to providing you with an exceptional driving experience.
                    </p>
                    
                    <div class="booking-details">
                        <div class="detail-item">
                            <span>Customer Name:</span>
                            <span>{{userName}}</span>
                        </div>
                        <div class="detail-item">
                            <span>Vehicle:</span>
                            <span>{{carName}}</span>
                        </div>
                        <div class="detail-item">
                            <span>Start Date:</span>
                            <span>{{startDate}}</span>
                        </div>
                        <div class="detail-item">
                            <span>End Date:</span>
                            <span>{{endDate}}</span>
                        </div>
                        <div class="detail-item">
                            <span>Booking Reference:</span>
                            <span>{{id}}</span>
                        </div>
                    </div>

                    <a href="/cars" class="btn btn-primary">BACK TO CARS</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
