<!DOCTYPE html>
<html>
<head>
    <title>Elite Rentals - Luxury Car Rental</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" />
    <style>
        :root {
            --primary: #c9a959;
            --primary-dark: #b38f4a;
            --secondary: #333;
            --dark: #111;
            --darker: #000;
            --light: #fff;
            --gray: #999;
            --transition: all 0.3s ease;
        }

        body {
            background-color: var(--darker);
            font-family: 'Montserrat', sans-serif;
            color: var(--light);
            min-height: 100vh;
            overflow-x: hidden;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Playfair Display', serif;
        }

        .navbar {
            background: rgba(0, 0, 0, 0.9);
            padding: 1.2rem 0;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            backdrop-filter: blur(10px);
            transition: var(--transition);
            border-bottom: 1px solid rgba(201, 169, 89, 0.1);
        }

        .navbar.scrolled {
            padding: 0.8rem 0;
            background: rgba(0, 0, 0, 0.95);
        }

        .navbar-brand {
            color: var(--primary) !important;
            font-size: 1.8rem;
            font-weight: 500;
            letter-spacing: 2px;
            font-family: 'Playfair Display', serif;
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
        }

        .nav-links {
            display: flex;
            gap: 10px;
            margin: 0;
            align-items: center;
        }

        .nav-links a {
            color: var(--light);
            text-decoration: none;
            font-size: 0.9rem;
            letter-spacing: 1px;
            transition: var(--transition);
            padding: 8px 15px;
            border-radius: 4px;
            font-weight: 500;
            position: relative;
        }

        .nav-links a:hover {
            color: var(--primary);
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            background: var(--primary);
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            transition: var(--transition);
        }

        .nav-links a:hover::after {
            width: 70%;
        }

        .nav-links .btn-nav {
            background: var(--primary);
            color: var(--darker);
            padding: 8px 20px;
            border-radius: 30px;
            font-weight: 600;
            margin-left: 10px;
            border: 2px solid var(--primary);
        }

        .nav-links .btn-nav:hover {
            background: transparent;
            color: var(--primary);
        }

        .nav-links .btn-nav::after {
            display: none;
        }

        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background: rgba(0, 0, 0, 0.95);
            min-width: 200px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.2);
            z-index: 1;
            border-radius: 4px;
            overflow: hidden;
            top: 100%;
            left: 50%;
            transform: translateX(-50%);
            margin-top: 10px;
            border: 1px solid rgba(201, 169, 89, 0.1);
        }

        .dropdown:hover .dropdown-content {
            display: block;
            animation: fadeIn 0.3s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-10px) translateX(-50%); }
            to { opacity: 1; transform: translateY(0) translateX(-50%); }
        }

        .dropdown-content a {
            color: var(--light);
            padding: 12px 16px;
            text-decoration: none;
            display: block;
            transition: var(--transition);
            text-align: left;
            border-bottom: 1px solid rgba(201, 169, 89, 0.1);
        }

        .dropdown-content a:hover {
            background-color: rgba(201, 169, 89, 0.1);
            color: var(--primary);
        }

        .dropdown-content a::after {
            display: none;
        }

        .theme-toggle {
            background: transparent;
            border: none;
            color: var(--light);
            font-size: 1.2rem;
            cursor: pointer;
            margin-left: 15px;
            transition: var(--transition);
        }

        .theme-toggle:hover {
            color: var(--primary);
            transform: rotate(30deg);
        }
        .hero-section {
            height: 85vh; /* Reduced height */
            min-height: 550px;
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('https://images.unsplash.com/photo-1503376780353-7e6692767b70?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80');
            background-size: cover;
            background-position: center;
            background-attachment: scroll; /* Better for mobile */
            display: flex;
            align-items: center;
            position: relative;
            margin-bottom: 0; /* Remove bottom margin */
        }
        .hero-content {
            position: relative;
            z-index: 2;
        }
        .hero-title {
            color: #c9a959;
            font-size: 4rem; /* Slightly smaller */
            font-weight: 400;
            letter-spacing: 3px;
            margin-bottom: 15px; /* Reduced */
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }
        .hero-subtitle {
            color: #fff;
            font-size: 1.6rem; /* Slightly smaller */
            font-weight: 300;
            letter-spacing: 2px;
            margin-bottom: 30px; /* Reduced */
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
        }
        .hero-buttons {
            margin-bottom: 20px; /* Added margin */
        }
        .btn-primary {
            background: #c9a959;
            border: none;
            color: #000;
            padding: 15px 40px;
            border-radius: 0;
            font-weight: 500;
            letter-spacing: 2px;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            text-transform: uppercase;
        }
        .btn-primary:hover {
            background: #b38f4a;
            color: #000;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(201, 169, 89, 0.3);
        }
        .features-section {
            padding: 60px 0; /* Reduced padding */
            background-color: var(--darker);
        }
        .feature-card {
            background: #0a0a0a;
            border: 1px solid #222;
            padding: 30px 25px; /* Reduced padding */
            text-align: center;
            margin-bottom: 25px; /* Reduced */
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            height: 100%; /* Make all cards same height */
        }
        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, transparent, rgba(201, 169, 89, 0.1), transparent);
            transform: translateX(-100%);
            transition: 0.5s;
        }
        .feature-card:hover::before {
            transform: translateX(100%);
        }
        .feature-card:hover {
            border-color: #c9a959;
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(201, 169, 89, 0.1);
        }
        .feature-icon {
            color: #c9a959;
            font-size: 3rem;
            margin-bottom: 25px;
            transition: all 0.3s ease;
        }
        .feature-card:hover .feature-icon {
            transform: scale(1.1);
        }
        .feature-title {
            color: #c9a959;
            font-size: 1.8rem;
            font-weight: 400;
            margin-bottom: 20px;
            letter-spacing: 1px;
        }
        .feature-text {
            color: #999;
            font-size: 1.1rem;
            line-height: 1.8;
        }
        .section-title {
            color: #c9a959;
            font-size: 2.5rem; /* Smaller */
            font-weight: 400;
            text-align: center;
            margin-bottom: 40px; /* Reduced */
            letter-spacing: 2px;
            position: relative;
        }
        .section-title::after {
            content: '';
            display: block;
            width: 80px;
            height: 2px;
            background: #c9a959;
            margin: 15px auto; /* Reduced */
        }
        .footer {
            background: #0a0a0a;
            padding: 30px 0; /* Reduced padding */
            border-top: 1px solid #222;
        }
        .footer-text {
            color: #999;
            text-align: center;
            font-size: 0.9rem;
            letter-spacing: 1px;
        }
        .scroll-down {
            position: absolute;
            bottom: 20px; /* Moved up */
            left: 50%;
            transform: translateX(-50%);
            color: var(--light);
            font-size: 1.5rem;
            cursor: pointer;
            animation: bounce 2s infinite;
            z-index: 10;
        }
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-20px);
            }
            60% {
                transform: translateY(-10px);
            }
        }
        .stats-section {
            background: #0a0a0a;
            padding: 50px 0; /* Reduced padding */
            border-top: 1px solid #222;
            border-bottom: 1px solid #222;
            margin-top: 0; /* Remove top margin */
        }
        .stat-item {
            text-align: center;
            padding: 20px 15px;
        }
        .stat-icon {
            color: var(--primary);
            font-size: 2.5rem;
            margin-bottom: 15px;
        }
        .stat-number {
            color: var(--light);
            font-size: 2.5rem;
            font-weight: 600;
            margin-bottom: 5px;
            font-family: 'Playfair Display', serif;
        }
        .stat-label {
            color: var(--gray);
            font-size: 0.9rem;
            letter-spacing: 2px;
        }
        .crud-section {
            padding: 100px 0;
            background: #111;
        }
        .crud-form {
            background: #000;
            border: 1px solid #333;
            padding: 40px;
            margin-bottom: 30px;
        }
        .form-label {
            color: #c9a959;
            font-size: 1.1rem;
            margin-bottom: 10px;
            letter-spacing: 1px;
        }
        .form-control {
            background: #111;
            border: 1px solid #333;
            color: #fff;
            padding: 12px;
            margin-bottom: 20px;
            border-radius: 0;
        }
        .form-control:focus {
            background: #111;
            border-color: #c9a959;
            color: #fff;
            box-shadow: none;
        }
        .form-select {
            background: #111;
            border: 1px solid #333;
            color: #fff;
            padding: 12px;
            margin-bottom: 20px;
            border-radius: 0;
        }
        .form-select:focus {
            background: #111;
            border-color: #c9a959;
            color: #fff;
            box-shadow: none;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <div class="nav-container">
                <a class="navbar-brand" href="/">
                    ELITE RENTALS
                </a>
                <div class="nav-links">
                    <a href="/">Home</a>

                    <div class="dropdown">
                        <a href="/cars">Our Fleet</a>
                        <div class="dropdown-content">
                            <a href="/cars?category=Sedan">Sedans</a>
                            <a href="/cars?category=SUV">SUVs</a>
                            <a href="/cars?category=Sports%20Car">Sports Cars</a>
                            <a href="/cars?category=Luxury">Luxury Cars</a>
                            <a href="/cars?category=Electric">Electric Cars</a>
                        </div>
                    </div>

                    {{!-- <div class="dropdown">
                        <a href="/services">Services</a>
                        <div class="dropdown-content">
                            <a href="/special-offers">Special Offers</a>
                            <a href="/locations">Pickup Locations</a>
                            <a href="/bookings">Manage Bookings</a>
                        </div>
                    </div> --}}

                    {{!-- <a href="/dashboard">Dashboard</a>
                    <a href="/faq">FAQ</a>
                    <a href="/contact">Contact</a>
                    <a href="/wishlist" class="ms-2"><i class="fas fa-heart"></i></a>
                    <button class="theme-toggle" id="theme-toggle">
                        <i class="fas fa-moon"></i> --}}
                    </button>
                    <a href="/bookings/new" class="btn-nav">Book Now</a>
                </div>
            </div>
        </div>
    </nav>

    <section class="hero-section">
        <div class="container">
            <div class="hero-content text-center" data-aos="fade-up" data-aos-duration="1000">
                <h1 class="hero-title">EXPERIENCE LUXURY</h1>
                <p class="hero-subtitle">Drive the world's most prestigious vehicles</p>
                <div class="hero-buttons">
                    <a href="/cars" class="btn btn-primary me-3">EXPLORE OUR FLEET</a>
                    <a href="/special-offers" class="btn btn-outline">SPECIAL OFFERS</a>
                </div>
            </div>
        </div>
        <div class="scroll-down">
            <i class="fas fa-chevron-down"></i>
        </div>
    </section>

    <section class="stats-section">
        <div class="container">
            <div class="row">
                <div class="col-md-3" data-aos="fade-up" data-aos-delay="100">
                    <div class="stat-item">
                        <div class="stat-icon">
                            <i class="fas fa-car-side"></i>
                        </div>
                        <div class="stat-number">50+</div>
                        <div class="stat-label">LUXURY VEHICLES</div>
                    </div>
                </div>
                <div class="col-md-3" data-aos="fade-up" data-aos-delay="200">
                    <div class="stat-item">
                        <div class="stat-icon">
                            <i class="fas fa-users"></i>
                        </div>
                        <div class="stat-number">1000+</div>
                        <div class="stat-label">HAPPY CLIENTS</div>
                    </div>
                </div>
                <div class="col-md-3" data-aos="fade-up" data-aos-delay="300">
                    <div class="stat-item">
                        <div class="stat-icon">
                            <i class="fas fa-headset"></i>
                        </div>
                        <div class="stat-number">24/7</div>
                        <div class="stat-label">SUPPORT</div>
                    </div>
                </div>
                <div class="col-md-3" data-aos="fade-up" data-aos-delay="400">
                    <div class="stat-item">
                        <div class="stat-icon">
                            <i class="fas fa-star"></i>
                        </div>
                        <div class="stat-number">5★</div>
                        <div class="stat-label">RATING</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="features-section">
        <div class="container">
            <h2 class="section-title" data-aos="fade-up">WHY CHOOSE US</h2>
            <div class="row">
                <div class="col-md-4" data-aos="fade-up" data-aos-delay="100">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-car"></i>
                        </div>
                        <h3 class="feature-title">PREMIUM FLEET</h3>
                        <p class="feature-text">Choose from our exclusive collection of luxury and sports cars, each maintained to the highest standards of excellence.</p>
                        <a href="/cars" class="feature-link">View Our Fleet <i class="fas fa-arrow-right"></i></a>
                    </div>
                </div>
                <div class="col-md-4" data-aos="fade-up" data-aos-delay="200">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-concierge-bell"></i>
                        </div>
                        <h3 class="feature-title">ELITE SERVICE</h3>
                        <p class="feature-text">Experience personalized service with our dedicated team of luxury car specialists, available around the clock.</p>
                        <a href="/contact" class="feature-link">Contact Us <i class="fas fa-arrow-right"></i></a>
                    </div>
                </div>
                <div class="col-md-4" data-aos="fade-up" data-aos-delay="300">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <i class="fas fa-shield-alt"></i>
                        </div>
                        <h3 class="feature-title">PEACE OF MIND</h3>
                        <p class="feature-text">Enjoy comprehensive insurance coverage and 24/7 roadside assistance with every rental, ensuring a worry-free experience.</p>
                        <a href="/faq" class="feature-link">Learn More <i class="fas fa-arrow-right"></i></a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Featured Vehicles Section Styling -->
    <style>
        .featured-cars-section {
            padding: 60px 0;
            background-color: #070707;
        }
        
        .car-card {
            background: #0a0a0a;
            border: 1px solid #222;
            margin-bottom: 30px;
            overflow: hidden;
            position: relative;
            height: 100%;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .car-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
            border-color: #c9a959;
        }
        
        .car-image-container {
            position: relative;
            overflow: hidden;
            height: 220px;
        }
        
        .car-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }
        
        .car-card:hover .car-image {
            transform: scale(1.05);
        }
        
        .car-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transition: opacity 0.3s ease;
        }
        
        .car-card:hover .car-overlay {
            opacity: 1;
        }
        
        .btn-view {
            background: #c9a959;
            color: #000;
            padding: 10px 20px;
            text-decoration: none;
            text-transform: uppercase;
            font-weight: 600;
            letter-spacing: 1px;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }
        
        .btn-view:hover {
            background: #b38f4a;
            color: #000;
        }
        
        .car-badge {
            position: absolute;
            top: 15px;
            left: 15px;
            background: #c9a959;
            color: #000;
            padding: 5px 10px;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            z-index: 2;
        }
        
        .btn-wishlist {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(0,0,0,0.5);
            border: none;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-size: 1.2rem;
            z-index: 2;
            transition: all 0.3s ease;
            cursor: pointer;
        }
        
        .btn-wishlist:hover {
            background: #c9a959;
            color: #000;
        }
        
        .car-details {
            padding: 20px;
        }
        
        .car-name {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: #c9a959;
            margin-bottom: 10px;
        }
        
        .car-rating {
            margin-bottom: 10px;
        }
        
        .car-rating i {
            color: #c9a959;
            margin-right: 2px;
        }
        
        .car-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 15px;
            font-size: 0.9rem;
            color: #999;
        }
        
        .car-info span {
            display: flex;
            align-items: center;
        }
        
        .car-info i {
            margin-right: 5px;
            color: #c9a959;
        }
        
        .car-price {
            display: flex;
            align-items: baseline;
            margin-bottom: 15px;
        }
        
        .price-label {
            font-size: 0.9rem;
            color: #999;
            margin-right: 5px;
        }
        
        .price-value {
            font-size: 1.5rem;
            color: #fff;
            font-weight: 600;
            margin-right: 5px;
        }
        
        .price-period {
            font-size: 0.9rem;
            color: #999;
        }
        
        .car-actions {
            display: flex;
            gap: 10px;
        }
        
        .btn-details, .btn-book {
            flex: 1;
            text-align: center;
            padding: 10px;
            text-decoration: none;
            text-transform: uppercase;
            font-size: 0.8rem;
            font-weight: 600;
            letter-spacing: 1px;
            transition: all 0.3s ease;
        }
        
        .btn-details {
            background: #1a1a1a;
            color: #c9a959;
            border: 1px solid #c9a959;
        }
        
        .btn-details:hover {
            background: #c9a959;
            color: #000;
        }
        
        .btn-book {
            background: #c9a959;
            color: #000;
        }
        
        .btn-book:hover {
            background: #b38f4a;
        }
        
        .btn-view-all {
            background: transparent;
            border: 2px solid #c9a959;
            color: #c9a959;
            padding: 12px 30px;
            font-size: 1rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 2px;
            transition: all 0.3s ease;
            display: inline-block;
            margin-top: 20px;
        }
        
        .btn-view-all:hover {
            background: #c9a959;
            color: #000;
        }
        
        @media (max-width: 768px) {
            .car-info {
                flex-direction: column;
                gap: 5px;
            }
            
            .car-actions {
                flex-direction: column;
            }
        }
    </style>

    <!-- Featured Vehicles Section HTML -->
    <section class="featured-cars-section">
        <div class="container">
            <h2 class="section-title" data-aos="fade-up">FEATURED VEHICLES</h2>
            <div class="row">
                {{#each cars}}
                    {{#if (lt @index 3)}}
                    <div class="col-md-4" data-aos="fade-up" data-aos-delay="{{multiply @index 100}}">
                        <div class="car-card">
                            <div class="car-image-container">
                                <img src="{{this.image}}" alt="{{this.name}}" class="car-image">
                                <div class="car-overlay">
                                    <a href="/cars/{{this.id}}" class="btn-view">View Details</a>
                                </div>
                                <div class="car-badge">{{this.category}}</div>
                                <form action="/wishlist/add/{{this.id}}" method="POST" class="wishlist-form">
                                    <button type="submit" class="btn-wishlist">
                                        <i class="far fa-heart"></i>
                                    </button>
                                </form>
                            </div>
                            <div class="car-details">
                                <h3 class="car-name">{{this.name}}</h3>
                                <div class="car-rating">
                                    {{#times this.rating}}
                                        <i class="fas fa-star"></i>
                                    {{/times}}
                                </div>
                                <div class="car-info">
                                    <span><i class="fas fa-cog"></i> {{this.transmission}}</span>
                                    <span><i class="fas fa-gas-pump"></i> {{this.fuelType}}</span>
                                    <span><i class="fas fa-users"></i> {{this.seats}} seats</span>
                                </div>
                                <div class="car-price">
                                    <span class="price-label">From</span>
                                    <span class="price-value">${{this.pricePerDay}}</span>
                                    <span class="price-period">/ day</span>
                                </div>
                                <div class="car-actions">
                                    <a href="/cars/{{this.id}}" class="btn-details">Details</a>
                                    <a href="/bookings/new?carId={{this.id}}" class="btn-book">Book Now</a>
                                </div>
                            </div>
                        </div>
                    </div>
                    {{/if}}
                {{/each}}
            </div>
            <div class="text-center mt-4" data-aos="fade-up">
                <a href="/cars" class="btn-view-all">VIEW ALL VEHICLES <i class="fas fa-arrow-right ms-2"></i></a>
            </div>
        </div>
    </section>

    <section class="crud-section">
        <div class="container">
            <h2 class="section-title">ADD NEW VEHICLE</h2>
            <div class="row justify-content-center">
                <div class="col-md-8">
                    <div class="crud-form">
                        <form action="/cars" method="POST">
                            <div class="mb-3">
                                <label for="id" class="form-label">Vehicle ID</label>
                                <input type="text" class="form-control" id="id" name="id" required>
                            </div>
                            <div class="mb-3">
                                <label for="name" class="form-label">Vehicle Name</label>
                                <input type="text" class="form-control" id="name" name="name" required>
                            </div>
                            <div class="mb-3">
                                <label for="brand" class="form-label">Brand</label>
                                <input type="text" class="form-control" id="brand" name="brand" required>
                            </div>
                            <div class="mb-3">
                                <label for="pricePerDay" class="form-label">Price Per Day</label>
                                <input type="number" class="form-control" id="pricePerDay" name="pricePerDay" required>
                            </div>
                            <div class="mb-3">
                                <label for="available" class="form-label">Available</label>
                                <select class="form-select" id="available" name="available" required>
                                    <option value="true">Yes</option>
                                    <option value="false">No</option>
                                </select>
                            </div>
                            <button type="submit" class="btn btn-primary w-100">ADD VEHICLE</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer class="footer">
        <div class="container">
            <p class="footer-text">© 2024 ELITE RENTALS. ALL RIGHTS RESERVED.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
    <script>
        // Initialize AOS
        AOS.init({
            duration: 800,
            once: true
        });
        
        // Improve smooth scrolling behavior
        document.querySelector('.scroll-down').addEventListener('click', function() {
            document.querySelector('.stats-section').scrollIntoView({
                behavior: 'smooth',
                block: 'start'
            });
        });
        
        // Add scroll event listener to adjust navbar
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });
        
        // Initialize theme toggle if it exists
        const themeToggle = document.getElementById('theme-toggle');
        if (themeToggle) {
            themeToggle.addEventListener('click', function() {
                document.documentElement.classList.toggle('dark-mode');
                const isDarkMode = document.documentElement.classList.contains('dark-mode');
                localStorage.setItem('darkMode', isDarkMode);
                this.innerHTML = isDarkMode ? '<i class="fas fa-sun"></i>' : '<i class="fas fa-moon"></i>';
            });
            
            // Check for saved theme preference
            const savedDarkMode = localStorage.getItem('darkMode') === 'true';
            if (savedDarkMode) {
                document.documentElement.classList.add('dark-mode');
                themeToggle.innerHTML = '<i class="fas fa-sun"></i>';
            }
        }
    </script>
</body>
</html>
